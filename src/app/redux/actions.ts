import { createAction, props } from '@ngrx/store';
import { UnitType } from './reducers';

// export const buildUnitAction = createAction(
//   'BUILD UNIT',
//   props<{
//     type: UnitType;
//   }>()
// );
export const balanceAction = createAction('BUILD UNIT');
